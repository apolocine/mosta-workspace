# __PROJECT__

App générée par `@mostajs/workspace`.

## Quickstart

```bash
npm install
npm run dev
```

Health-check : http://localhost:${PORT}/api/health

## Modules @mostajs pré-installés

config · data-plug · orm · auth · mailer · rbac · storage · subscriptions-plan · qrpanel · pwa-scan · payment · pm2

Bootstrap chaîné dans `lib/bootstrap.ts` — décommenter et configurer chaque section selon les besoins.

## Workflow multi-env

Voir `../WORKFLOW.md`.
